#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_anywhere@1.5.0@anywhere/bin/anywhere" "$@"
  ret=$?
else 
  node  "$basedir/../_anywhere@1.5.0@anywhere/bin/anywhere" "$@"
  ret=$?
fi
exit $ret
